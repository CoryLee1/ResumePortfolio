PORT=3001
# Comma-separated origins for browser / Expo (production: your Vercel URL)
CORS_ORIGIN=http://localhost:5173,http://localhost:8081

# Future: OpenAI / Gemini / etc. (never commit real keys)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
